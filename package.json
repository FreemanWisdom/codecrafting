{
  "name": "codecrafting-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix codecrafting",
    "build": "npm run build --prefix codecrafting && node -e \"const fs = require('fs'); fs.rmSync('dist', { recursive: true, force: true }); fs.cpSync('codecrafting/dist', 'dist', { recursive: true });\""
  }
}
