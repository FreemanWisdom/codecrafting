{
  "name": "codecrafting-root",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "codecrafting"
  ],
  "scripts": {
    "build": "npm run build --workspace=codecrafting && node -e \"const fs = require('fs'); fs.rmSync('dist', { recursive: true, force: true }); fs.cpSync('codecrafting/dist', 'dist', { recursive: true });\""
  }
}
